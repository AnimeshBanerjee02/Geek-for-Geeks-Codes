#User function Template for python3

class Solution:

   def nonRepeatingChar(self,s):
        d={}
        for i in s:
            d[i]=d.get(i,0)+1
        for i,j in d.items():
            if j==1:
                return i
        return '$'
