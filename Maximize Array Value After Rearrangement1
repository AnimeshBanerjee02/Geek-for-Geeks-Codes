#User function Template for python3

class Solution:
    def Maximize(self, arr): 
        temp = sorted(arr)
        idx = 0
        sum1 = 0
        for i in temp:
            sum1 = sum1 + (i*idx)
            idx +=1
        return sum1%(10**9 + 7)
