#User function Template for python3
class Solution:
    
    def missingNumber(self, n, arr):
        total_sum = n * (n + 1) // 2
        
        array_sum = sum(arr)
        
        return total_sum - array_sum
