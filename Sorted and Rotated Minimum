#User function Template for python3

class Solution:
    def findMin(self, arr):
        min_val = arr[0]
        
        for num in arr:
            if num < min_val:
                min_val = num
        
        return min_val
