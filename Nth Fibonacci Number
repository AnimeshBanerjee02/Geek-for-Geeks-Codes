
class Solution:
    def nthFibonacci(self, n: int) -> int:
        fib = [0] * (n + 1)
        fib[0], fib[1] = 0, 1

        for i in range(2, n + 1):
            fib[i] = (fib[i - 1] + fib[i - 2]) % 1000000007

        return fib[n]
 

